# MaxIOFS Configuration Example
# Copy this file to .env and customize for your environment

# ============================================
# BACKEND CONFIGURATION
# ============================================

# Public URLs (used for redirects, presigned URLs, CORS, etc.)
# These should match your public-facing URLs
MAXIOFS_PUBLIC_API_URL=http://localhost:8080
MAXIOFS_PUBLIC_CONSOLE_URL=http://localhost:8081

# For production with custom domain:
# MAXIOFS_PUBLIC_API_URL=https://s3.example.com
# MAXIOFS_PUBLIC_CONSOLE_URL=https://console.example.com

# CORS Allowed Origins (comma-separated list)
# Default: http://localhost:3000,http://localhost:3001,http://localhost:3002
MAXIOFS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002

# For production with custom domain:
# MAXIOFS_ALLOWED_ORIGINS=https://console.example.com,https://app.example.com

# Server Listen Addresses
MAXIOFS_LISTEN=:8080           # S3 API port
MAXIOFS_CONSOLE_LISTEN=:8081   # Web Console API port

# Data Directory
MAXIOFS_DATA_DIR=./data

# Log Level (debug, info, warn, error)
MAXIOFS_LOG_LEVEL=info

# TLS Configuration (optional)
MAXIOFS_ENABLE_TLS=false
MAXIOFS_CERT_FILE=/path/to/cert.pem
MAXIOFS_KEY_FILE=/path/to/key.pem

# ============================================
# FRONTEND CONFIGURATION
# ============================================

# API URLs for frontend (Next.js)
NEXT_PUBLIC_API_URL=http://localhost:8081/api/v1
NEXT_PUBLIC_S3_URL=http://localhost:8080

# For production with custom domain:
# NEXT_PUBLIC_API_URL=https://console.example.com/api/v1
# NEXT_PUBLIC_S3_URL=https://s3.example.com

# ============================================
# EXAMPLE PRODUCTION SETUP
# ============================================
#
# Let's say your production setup is:
# - S3 API: https://s3.mycompany.com
# - Web Console: https://storage-console.mycompany.com
#
# Backend (.env in project root):
# MAXIOFS_PUBLIC_API_URL=https://s3.mycompany.com
# MAXIOFS_PUBLIC_CONSOLE_URL=https://storage-console.mycompany.com
# MAXIOFS_ALLOWED_ORIGINS=https://storage-console.mycompany.com
# MAXIOFS_ENABLE_TLS=true
# MAXIOFS_CERT_FILE=/etc/ssl/certs/mycompany.pem
# MAXIOFS_KEY_FILE=/etc/ssl/private/mycompany.key
#
# Frontend (web/frontend/.env.local):
# NEXT_PUBLIC_API_URL=https://storage-console.mycompany.com/api/v1
# NEXT_PUBLIC_S3_URL=https://s3.mycompany.com
